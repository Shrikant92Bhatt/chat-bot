{
  "name": "sandbox-service",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone isolated-vm code execution service for the chat-api code_interpreter tool. Deployed separately from chat-api because isolated-vm is a native addon that cannot run inside a Vercel serverless function.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts"
  },
  "dependencies": {
    "express": "^4.19.2",
    "isolated-vm": "^7.0.1",
    "typescript": "~6.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.13.3",
    "ts-node": "^10.9.2"
  }
}
